spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

  mail:
    host: mail.gmx.net
    port: 587
    username: quiz_rh@gmx.de
    password: deinPasswort
    from: Trivify

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        debug: true