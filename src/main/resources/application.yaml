spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: webworksachenbach@gmail.com
    password: xnet thyc pjtt yvua
    name: Paul Achenbach
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true